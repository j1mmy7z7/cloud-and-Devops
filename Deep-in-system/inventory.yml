kvm_hosts:
  hosts:
    kvm_target:
      ansible_host: 192.168.122.159
    
  vars:
    ansible_user: "{{ user }}"
    ansible_port: 22
    ansible_password: "{{ password }}"
    ansible_become_password: "{{ password }}"
